<%= form_for(@program_chef) do |f| %>
  <% if @program_chef.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@program_chef.errors.count, "error") %> prohibited this program_chef from being saved:</h2>

      <ul>
      <% @program_chef.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
